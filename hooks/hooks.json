{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": ".*review.*",
        "hooks": [
          { "type": "command", "command": "node \"${CLAUDE_PLUGIN_ROOT}/dist/index.js\" run \"Please ensure the review has passed. Do not proceed until any critical issues raised by Trae Agent have been resolved.\"" }
        ]
      }
    ]
  }
}
