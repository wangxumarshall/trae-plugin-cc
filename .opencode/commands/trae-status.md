---
description: Views status of background Trae tasks (running, completed, or cancelled).
---

Check background task status by running:

```bash
node "${CLAUDE_PLUGIN_ROOT}/dist/index.js" status
```

Shows all background tasks with their IDs, timestamps, and current status (running/completed/cancelled).
